spring:
  datasource:
    url: "jdbc:mysql://${MYSQL_HOST:localhost}:3306/demo?useSSL=false"
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driverClassName: com.mysql.cj.jdbc.Driver
logging:
  level:
    org:
      springframework:
        web: DEBUG
        jdbc:
          core: TRACE


